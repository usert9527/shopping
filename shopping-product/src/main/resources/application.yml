spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://111.231.72.163:3306/shopping-pms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 106.53.74.231:8848
  application:
    name: shopping-product
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 10001

logging:
  level:
    com.user9527.shopping.product: debug

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.user9527.shopping.product.dao.AttrGroupDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.user9527.shopping.product.entity.AttrGroupEntity" id="attrGroupMap">
        <result property="attrGroupId" column="attr_group_id"/>
        <result property="attrGroupName" column="attr_group_name"/>
        <result property="sort" column="sort"/>
        <result property="descript" column="descript"/>
        <result property="icon" column="icon"/>
        <result property="catelogId" column="catelog_id"/>
    </resultMap>

    <resultMap id="spuItemAttrGroupVo" type="com.user9527.shopping.product.vo.SpuItemAttrGroupVo">
        <result property="groupName" column="attr_group_name"/>
        <collection property="attrs" ofType="com.user9527.shopping.product.vo.Attr">
            <result property="attrName" column="attr_name"/>
            <result property="attrValue" column="attr_value"/>
        </collection>
    </resultMap>

    <select id="getAttrGroupWithAttrsBySpuId" resultMap="spuItemAttrGroupVo">
        select ag.attr_group_id,ag.attr_group_name,agr.attr_id,atr.attr_name,pav.attr_value from pms_attr_group ag
        LEFT JOIN pms_attr_attrgroup_relation agr
          on ag.attr_group_id = agr.attr_group_id
        LEFT JOIN pms_attr atr
          on agr.attr_id = atr.attr_id
        LEFT JOIN pms_product_attr_value pav
            on agr.attr_id = pav.attr_id
        where ag.catelog_id = #{catalogId} and spu_id = #{spuId}
    </select>


</mapper>